# 智慧宿舍报修平台 - 下一步开发计划

> 📅 计划制定时间：2025-12-06  
> 📊 当前进度：工单创建功能已完成  
> 🎯 下一阶段目标：完善工单管理功能

---

## 📋 已完成功能清单

### ✅ 第一阶段：基础架构（已完成）
- [x] 数据库设计与迁移（用户、角色、权限、工单表）
- [x] JWT 身份认证系统
- [x] 用户登录功能（前后端）
- [x] 工单列表展示（分页、筛选）
- [x] 工单创建功能（支持图片上传）

---

## 🚀 第二阶段：工单详情与状态管理（预计 4-6 小时）

### 1️⃣ 工单详情页面（90分钟）

#### 后端 API（30分钟）
**文件：** `后端/SmartDormitoryRepair.Api/OrdersController.cs`

```csharp
// 添加获取工单详情接口
[HttpGet("{id}")]
public async Task<ActionResult> GetOrderById(int id)
{
    var order = await _context.Orders.FindAsync(id);
    if (order == null) return NotFound("工单不存在");
    
    return Ok(order);
}
```

#### 前端页面（60分钟）
**文件：** `前端/SmartDormitoryRepair.Web/src/views/OrderDetail.vue`

**功能点：**
- 展示工单完整信息（标题、位置、描述、创建时间、状态）
- 图片预览（支持放大查看）
- 状态流程展示（待处理 → 处理中 → 已完成）
- 操作记录时间线
- 返回按钮

**设计风格：**
- 采用现代化卡片布局
- 渐变色背景（与 Qoder 官网风格一致）
- 流畅的展开/收起动画

---

### 2️⃣ 工单状态管理优化（60分钟）

#### 后端优化（20分钟）
**文件：** `后端/SmartDormitoryRepair.Api/OrdersController.cs`

```csharp
// 优化状态更新接口，添加操作日志
[HttpPatch("{id}/status")]
public async Task<ActionResult> UpdateStatus(int id, [FromBody] UpdateStatusDto dto)
{
    var order = await _context.Orders.FindAsync(id);
    if (order == null) return NotFound("工单不存在");
    
    // 验证状态转换合法性
    if (!IsValidStatusTransition(order.Status, dto.Status))
        return BadRequest("非法的状态转换");
    
    order.Status = dto.Status;
    // TODO: 记录操作日志（后续扩展）
    await _context.SaveChangesAsync();
    
    return Ok(new { message = "状态更新成功" });
}
```

#### 前端状态管理组件（40分钟）
**文件：** `前端/SmartDormitoryRepair.Web/src/components/StatusManager.vue`

**功能点：**
- 状态徽章显示（不同颜色）
- 状态更新按钮（仅管理员可见）
- 状态变更确认对话框
- 操作成功提示（优雅的动画效果）

---

### 3️⃣ 工单指派功能（90分钟）

#### 数据库扩展（10分钟）
**文件：** `后端/SmartDormitoryRepair.Domain/Order.cs`

```csharp
// 已有字段 AssignedTo，添加维修人员信息扩展
public string? AssignedToName { get; set; }  // 指派维修人员姓名
public DateTime? AssignedTime { get; set; }  // 指派时间
```

#### 后端 API（30分钟）
**文件：** `后端/SmartDormitoryRepair.Api/OrdersController.cs`

```csharp
// 获取可用维修人员列表
[HttpGet("maintainers")]
[Authorize(Roles = "Admin,Manager")]
public async Task<ActionResult> GetMaintainers()
{
    var maintainers = await _context.Users
        .Where(u => u.Role == "Maintainer")
        .Select(u => new { u.Id, u.Username })
        .ToListAsync();
    
    return Ok(maintainers);
}

// 指派工单
[HttpPost("{id}/assign")]
[Authorize(Roles = "Admin,Manager")]
public async Task<ActionResult> AssignOrder(int id, [FromBody] AssignOrderDto dto)
{
    var order = await _context.Orders.FindAsync(id);
    if (order == null) return NotFound("工单不存在");
    
    var maintainer = await _context.Users.FindAsync(dto.MaintainerId);
    if (maintainer == null) return NotFound("维修人员不存在");
    
    order.AssignedTo = dto.MaintainerId;
    order.AssignedToName = maintainer.Username;
    order.AssignedTime = DateTime.Now;
    order.Status = "Processing";
    
    await _context.SaveChangesAsync();
    
    return Ok(new { message = "指派成功" });
}
```

#### 前端指派对话框（50分钟）
**文件：** `前端/SmartDormitoryRepair.Web/src/components/AssignDialog.vue`

**功能点：**
- 维修人员选择下拉框
- 备注信息输入
- 确认/取消按钮
- 加载状态显示

---

## 🎨 第三阶段：用户体验优化（预计 3-4 小时）

### 1️⃣ 工单评论/备注功能（120分钟）

#### 数据库设计（15分钟）
**新建文件：** `后端/SmartDormitoryRepair.Domain/OrderComment.cs`

```csharp
public class OrderComment
{
    public int Id { get; set; }
    public int OrderId { get; set; }
    public string Content { get; set; } = null!;
    public string Creator { get; set; } = null!;
    public DateTime CreateTime { get; set; } = DateTime.Now;
}
```

#### 后端 API（45分钟）
- 添加评论接口
- 获取评论列表接口
- 删除评论接口（仅创建者和管理员）

#### 前端评论组件（60分钟）
**文件：** `前端/SmartDormitoryRepair.Web/src/components/CommentSection.vue`

**功能点：**
- 评论列表展示（时间倒序）
- 评论输入框
- 删除按钮（使用 ✕ 图标，符合你的设计偏好）
- 实时刷新

---

### 2️⃣ 数据统计仪表板（120分钟）

#### 后端统计 API（40分钟）
**文件：** `后端/SmartDormitoryRepair.Api/StatisticsController.cs`

```csharp
[ApiController]
[Route("api/[controller]")]
[Authorize]
public class StatisticsController : ControllerBase
{
    // 获取工单统计数据
    [HttpGet("dashboard")]
    public async Task<ActionResult> GetDashboard()
    {
        var total = await _context.Orders.CountAsync();
        var pending = await _context.Orders.CountAsync(o => o.Status == "Pending");
        var processing = await _context.Orders.CountAsync(o => o.Status == "Processing");
        var completed = await _context.Orders.CountAsync(o => o.Status == "Completed");
        
        return Ok(new { total, pending, processing, completed });
    }
}
```

#### 前端仪表板页面（80分钟）
**文件：** `前端/SmartDormitoryRepair.Web/src/views/Dashboard.vue`

**功能点：**
- 数字卡片展示（总工单数、待处理、处理中、已完成）
- 状态分布饼图（可选，使用 ECharts）
- 最近工单列表
- 快捷操作按钮
- 采用现代化渐变色设计

---

### 3️⃣ 消息通知系统（90分钟）

#### 前端通知组件（90分钟）
**文件：** `前端/SmartDormitoryRepair.Web/src/components/NotificationBell.vue`

**功能点：**
- 右上角铃铛图标
- 未读消息红点提示
- 下拉通知列表
- 标记已读功能
- 优雅的弹出动画

**通知场景：**
- 工单被指派（维修人员）
- 工单状态变更（创建者）
- 新评论通知

---

## 🔐 第四阶段：权限与角色管理（预计 4-5 小时）

### 1️⃣ 角色管理页面（150分钟）

#### 后端 API（60分钟）
**文件：** `后端/SmartDormitoryRepair.Api/RolesController.cs`

**接口清单：**
- 获取角色列表
- 创建角色
- 更新角色
- 删除角色
- 获取角色权限
- 更新角色权限

#### 前端角色管理页面（90分钟）
**文件：** `前端/SmartDormitoryRepair.Web/src/views/RoleManagement.vue`

**功能点：**
- 角色列表表格
- 新建/编辑角色对话框
- 权限分配（树形复选框）
- 删除确认（使用 ✕ 图标按钮）

---

### 2️⃣ 用户管理页面（120分钟）

#### 后端 API（50分钟）
**文件：** `后端/SmartDormitoryRepair.Api/UsersController.cs`

**接口清单：**
- 获取用户列表
- 创建用户
- 更新用户信息
- 禁用/启用用户
- 重置密码

#### 前端用户管理页面（70分钟）
**文件：** `前端/SmartDormitoryRepair.Web/src/views/UserManagement.vue`

**功能点：**
- 用户列表表格
- 搜索筛选（用户名、角色）
- 新建/编辑用户对话框
- 状态标签（启用/禁用）
- 批量操作

---

## 📱 第五阶段：移动端适配与优化（预计 3-4 小时）

### 1️⃣ 响应式布局优化（120分钟）

**优化文件：**
- `OrderList.vue` - 移动端卡片布局
- `OrderDetail.vue` - 垂直布局适配
- `OrderCreate.vue` - 表单字段优化
- `Dashboard.vue` - 数据卡片堆叠

**关键技术：**
- CSS Media Queries
- Flexbox 弹性布局
- Element Plus 响应式组件

---

### 2️⃣ 图片压缩与优化（60分钟）

#### 后端图片处理（60分钟）
**功能点：**
- 上传时自动压缩图片
- 生成缩略图
- 限制文件大小（2MB）
- 支持格式验证

**技术方案：**
```csharp
// 使用 ImageSharp 库
using SixLabors.ImageSharp;
using SixLabors.ImageSharp.Processing;

// 压缩图片代码示例
image.Mutate(x => x.Resize(new ResizeOptions
{
    Size = new Size(1200, 1200),
    Mode = ResizeMode.Max
}));
```

---

## 🧪 第六阶段：测试与部署（预计 4-5 小时）

### 1️⃣ 单元测试（120分钟）

**测试框架：** xUnit + Moq

**测试覆盖：**
- AuthController 测试
- OrdersController 测试
- PasswordHasher 测试
- 权限验证测试

**文件结构：**
```
SmartDormitoryRepair.Tests/
├── Controllers/
│   ├── AuthControllerTests.cs
│   └── OrdersControllerTests.cs
└── Services/
    └── PasswordHasherTests.cs
```

---

### 2️⃣ 前端组件测试（可选，60分钟）

**测试框架：** Vitest + Vue Test Utils

**测试重点：**
- 登录表单验证
- 工单创建表单验证
- 状态更新组件

---

### 3️⃣ Docker 容器化（90分钟）

#### Dockerfile 配置（45分钟）

**后端 Dockerfile：**
```dockerfile
FROM mcr.microsoft.com/dotnet/aspnet:8.0 AS base
WORKDIR /app
EXPOSE 5002

FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build
WORKDIR /src
COPY ["SmartDormitoryRepair.Api/SmartDormitoryRepair.Api.csproj", "SmartDormitoryRepair.Api/"]
RUN dotnet restore
COPY . .
RUN dotnet publish -c Release -o /app/publish

FROM base AS final
WORKDIR /app
COPY --from=build /app/publish .
ENTRYPOINT ["dotnet", "SmartDormitoryRepair.Api.dll"]
```

**前端 Dockerfile：**
```dockerfile
FROM node:18 AS build
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
EXPOSE 80
```

#### Docker Compose（45分钟）

**文件：** `docker-compose.yml`

```yaml
version: '3.8'
services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: DormitoryRepair
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  backend:
    build: ./后端/SmartDormitoryRepair.Api
    ports:
      - "5002:5002"
    depends_on:
      - mysql
    environment:
      ConnectionStrings__Default: "Server=mysql;Database=DormitoryRepair;User=root;Password=root123;"

  frontend:
    build: ./前端/SmartDormitoryRepair.Web
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  mysql_data:
```

---

### 4️⃣ 生产环境部署（60分钟）

**部署方案：**
1. 服务器环境配置（Ubuntu/CentOS）
2. 安装 Docker 和 Docker Compose
3. 配置 Nginx 反向代理
4. SSL 证书配置（Let's Encrypt）
5. 自动化部署脚本

---

## 📊 功能优先级建议

### 🔥 高优先级（必须完成）
1. ✅ 工单详情页面
2. ✅ 工单指派功能
3. ✅ 状态管理优化
4. ✅ 数据统计仪表板

### 🌟 中优先级（建议完成）
1. 评论/备注功能
2. 用户管理页面
3. 角色权限管理
4. 移动端适配

### 💡 低优先级（可选）
1. 消息通知系统
2. 单元测试
3. Docker 容器化
4. 高级搜索功能

---

## 🎯 本周开发计划（建议）

### 周一（2小时）
- ✅ 工单详情页面（前后端）
- ✅ 路由配置

### 周二（2小时）
- ✅ 工单指派功能（后端 API）
- ✅ 维修人员管理

### 周三（2小时）
- ✅ 工单指派功能（前端对话框）
- ✅ 状态更新优化

### 周四（2小时）
- ✅ 数据统计仪表板（后端）
- ✅ 统计图表

### 周五（2小时）
- ✅ 评论功能（数据库设计）
- ✅ 评论 API 开发

### 周末（4小时）
- ✅ 评论功能（前端组件）
- ✅ 用户管理页面
- ✅ 整体测试与优化

---

## 🛠️ 技术栈补充建议

### 前端增强
- **ECharts**：数据可视化图表
- **dayjs**：时间处理工具
- **vueuse**：Vue 组合式 API 工具库
- **lodash**：JavaScript 工具函数库

### 后端增强
- **Serilog**：结构化日志记录
- **AutoMapper**：对象映射
- **FluentValidation**：数据验证
- **ImageSharp**：图片处理

### 开发工具
- **Postman**：API 测试
- **Swagger**：API 文档自动生成
- **ESLint + Prettier**：代码格式化

---

## 📝 代码规范建议

### 命名规范
- **C# 类名**：PascalCase（如 `OrdersController`）
- **C# 方法**：PascalCase（如 `GetOrderById`）
- **Vue 组件**：PascalCase（如 `OrderDetail.vue`）
- **变量/函数**：camelCase（如 `handleSubmit`）

### 注释规范
```csharp
/// <summary>
/// 获取工单详情
/// </summary>
/// <param name="id">工单ID</param>
/// <returns>工单详细信息</returns>
[HttpGet("{id}")]
public async Task<ActionResult> GetOrderById(int id)
{
    // 实现代码...
}
```

### Git 提交规范
```
feat: 添加工单详情页面
fix: 修复工单状态更新bug
style: 优化工单列表样式
refactor: 重构权限验证逻辑
docs: 更新 README 文档
test: 添加工单创建单元测试
```

---

## 🎨 设计规范（基于 Qoder 官网风格）

### 色彩方案
- **主色调**：`#409EFF`（蓝色）
- **渐变背景**：`linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%)`
- **成功色**：`#67C23A`
- **警告色**：`#E6A23C`
- **危险色**：`#F56C6C`

### 动画效果
- **过渡时间**：0.3s
- **缓动函数**：`cubic-bezier(0.4, 0, 0.2, 1)`
- **卡片阴影**：`box-shadow: 0 2px 12px 0 rgba(0, 0, 0, 0.1)`

### 布局规范
- **卡片圆角**：12px
- **按钮圆角**：4px
- **间距单位**：8px 的倍数（8, 16, 24, 32）

---

## 📞 技术支持

### 遇到问题时
1. **查看控制台错误信息**（F12 开发者工具）
2. **检查网络请求**（Network 标签）
3. **查看后端日志**（控制台输出）
4. **数据库验证**（Navicat 查询数据）

### 学习资源
- **Vue 3 官方文档**：https://cn.vuejs.org/
- **Element Plus 组件库**：https://element-plus.org/zh-CN/
- **ASP.NET Core 文档**：https://learn.microsoft.com/zh-cn/aspnet/core/
- **EF Core 文档**：https://learn.microsoft.com/zh-cn/ef/core/

---

## 🎉 结语

这份计划涵盖了从基础功能到高级特性的完整开发路线。你可以根据实际情况调整优先级和时间安排。

**建议按照以下顺序逐步实现：**
1. 先完成核心业务功能（工单详情、指派、评论）
2. 再优化用户体验（仪表板、通知）
3. 最后完善管理功能（用户、角色权限）
4. 部署前进行充分测试

**记住：**
- 每完成一个功能就提交代码
- 定期备份数据库
- 保持代码整洁和注释清晰
- 遵循你喜欢的设计风格（现代化、简洁优雅、渐变色彩）

祝开发顺利！🚀

---

> 📅 最后更新：2025-12-06  
> 👨‍💻 维护者：Qoder AI Assistant
